# Serve the React app for direct /app access
/app /app.html 200

# Serve the React app for all /app subroutes
/app/* /app.html 200

# Serve landing page for root
/ /index.html 200

# Catch-all: serve landing page for all other unmatched routes
/* /index.html 200