import React from 'react';
import { useSubscription } from './StripeIntegration';

const ProBadge = ({ className = "" }) => {
  // No badge needed since all features are free now
  return null;
};

export default ProBadge;